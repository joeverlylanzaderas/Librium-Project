[build]
  base = "frontend/web"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[ignore]
  # Ignore the mobile folder completely
  condition = "false"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200